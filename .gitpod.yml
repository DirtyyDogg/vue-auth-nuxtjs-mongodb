tasks:
  - init: npm install && npm install && echo "Replace me with a build script for the project."
    command: npm install && npm run dev && npm install && npm run dev && echo "Replace me with something that should run on every start, or just
      remove me entirely."
image: gitpod/workspace-mongodb
